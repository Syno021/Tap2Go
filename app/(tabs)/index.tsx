import { View } from 'react-native';
import ThemedText from '../../components/ThemedText';

export default function TabsIndex() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <ThemedText>Tab Index Page</ThemedText>
    </View>
  );
}
